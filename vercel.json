{
  "version": 2,
  "buildCommand": "pnpm run build:client && npx esbuild api/index.ts --bundle --platform=node --format=esm --packages=external --outfile=api/index.js --tsconfig=tsconfig.json",
  "outputDirectory": "public",
  "framework": null,
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
